using UnityEngine;
using System.Collections;
//using UnityEditor;

public static class EditorUtilityOpenFilePanel {
	
	public static string Apply () 
	{
		/*Object csv = Selection.activeObject;
		if (csv == null) 
		{
			EditorUtility.DisplayDialog("Select CSV", "You Must Select a csv file first!", "Ok");
			return "";
		}
	
		string path = EditorUtility.OpenFilePanel("Load CSV File", "", "csv");
		if (path.Length != 0) 
		{
			return path;
		}*/
		return "";
	}

}
